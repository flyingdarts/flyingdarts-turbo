{
  "name": "flyingdarts-auth",
  "version": "0.1.0",
  "scripts": {
    "build": "cargo build --release",
    "test": "cargo test"
  },
  "devDependencies": {},
  "engines": {
    "turbo": ">=1.10.0"
  }
}
