{
  "name": "flyingdarts.persistence",
  "version": "0.0.3",
  "private": false,
  "scripts": {
    "build": "dotnet build -c Debug --no-dependencies",
    "build:prod": "dotnet build --configuration Release",
    "publish": "dotnet nuget push 'bin/Release/Flyingdarts.Persistence.*.nupkg' -k $Env:NUGET_API_KEY -s https://api.nuget.org/v3/index.json --skip-duplicate"
  }
}
