{
  "name": "flyingdarts.frontend.flutter.features.keyboard",
  "version": "0.0.5",
  "description": "Keyboard feature package",
  "private": false,
  "scripts": {
    "build": " dart run build_runner build && flutter build"
  }
}
