{
  "name": "flyingdarts.frontend.flutter.features.profile",
  "version": "0.0.5",
  "description": "Profile feature package",
  "private": false,
  "scripts": {
    "build": " dart run build_runner build && flutter build"
  }
}
