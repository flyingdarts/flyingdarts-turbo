{
  "name": "flyingdarts_frontend_flutter_features_auth",
  "version": "0.0.5",
  "description": "Auth feature package",
  "private": false,
  "scripts": {
    "build": " dart run build_runner build && flutter build"
  }
}
