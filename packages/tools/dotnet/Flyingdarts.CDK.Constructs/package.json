{
  "name": "flyingdarts.cdk.constructs",
  "version": "0.0.5",
  "private": false,
  "scripts": {
    "build": "dotnet build -c Debug ",
    "build:prod": "dotnet build --configuration Release",
    "publish": "dotnet nuget push 'bin/Release/Flyingdarts.CDK.Constructs.*.nupkg' -k $Env:NUGET_API_KEY -s https://api.nuget.org/v3/index.json --skip-duplicate"
  },
  "dependencies": {
    "flyingdarts.persistence": "*"
  }
}
