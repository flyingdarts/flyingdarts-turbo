{
  "name": "flyingdarts.frontend.flutter.shared.internationalization",
  "version": "0.0.1",
  "description": "Internationalization package",
  "private": false,
  "scripts": {
    "build": "flutter --version",
    "build:prod": "flutter --version"
  }
}
