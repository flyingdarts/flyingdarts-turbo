meta {
  name: Delete service client access key
  type: http
  seq: 7
}

delete {
  url: {{baseUrl}}/v1/clients/:clientId/access-keys/:keyId
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}
