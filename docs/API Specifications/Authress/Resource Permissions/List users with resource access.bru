meta {
  name: List users with resource access
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/v1/resources/:resourceUri/users
  body: none
  auth: bearer
}

params:query {
  ~limit: 
  ~cursor: 
}

params:path {
  resourceUri: 
}

auth:bearer {
  token: {{token}}
}
