meta {
  name: Retrieve user connection credentials
  type: http
  seq: 6
}

get {
  url: {{baseUrl}}/v1/connections/:connectionId/users/:userId/credentials
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}
