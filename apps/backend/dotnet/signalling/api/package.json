{
  "name": "flyingdarts.signalling.api",
  "version": "1.0.0",
  "description": "Flyingdarts Signalling API Lambda Function",
  "private": true,
  "scripts": {
    "restore": "dotnet restore",
    "build": "dotnet build -c Debug",
    "build:prod": "dotnet build -c Release",
    "test": "dotnet test"
  },
  "dependencies": {
    "flyingdarts.dynamodb.service": "*",
    "flyingdarts.lambda.core": "*",
    "flyingdarts.meetings.service": "*",
    "flyingdarts.persistence": "*"
  }
}
