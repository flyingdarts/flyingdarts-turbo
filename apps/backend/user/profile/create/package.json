{
	"name": "flyingdarts.backend.user.profile.create",
	"version": "1.0.0",
	"private": true,
	"scripts": {
	  "build": "dotnet build -c Debug --no-dependencies",
	  "build:prod": "dotnet build -c Release --no-dependencies",
	  "deploy": "dotnet lambda deploy-function --function-name Flyingdarts-Backend-User-Profile-Create  -frun dotnet6 -f net6.0 --region eu-west-1"

	},
	"dependencies": {
		"flyingdarts.shared": "*",
		"flyingdarts.backend.shared": "*",
		"flyingdarts.persistence": "*"
	}
  }