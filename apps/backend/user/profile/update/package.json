{
	"name": "flyingdarts.backend.user.profile.update",
	"version": "1.0.0",
	"private": true,
	"scripts": {
	  "build": "dotnet build -c Debug --no-dependencies",
	  "build:prod": "dotnet build -c Release --no-dependencies",
	  "deploy:development": "dotnet lambda deploy-function --function-name Flyingdarts-Backend-User-Profile-Update-Development -frun dotnet6 -f net6.0 --region eu-west-1",
	  "deploy:production": "dotnet lambda deploy-function --function-name Flyingdarts-Backend-User-Profile-Update-Production -frun dotnet6 -f net6.0 --region eu-west-1"

	},
	"dependencies": {
		"flyingdarts.shared": "*",
		"flyingdarts.backend.shared": "*",
		"flyingdarts.persistence": "*"
	}
  }